<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<!--
  mapred-site.xml — Configuration MapReduce
  Framework : yarn  (exécution via YARN)
-->
<configuration>

  <!-- Utiliser YARN comme runtime pour MapReduce -->
  <property>
    <n>mapreduce.framework.name</n>
    <value>yarn</value>
    <description>Soumettre les jobs MR via YARN</description>
  </property>

  <!-- Adresse du Job History Server -->
  <property>
    <n>mapreduce.jobhistory.address</n>
    <value>bigdata-node:10020</value>
  </property>

  <!-- UI du Job History Server -->
  <property>
    <n>mapreduce.jobhistory.webapp.address</n>
    <value>bigdata-node:19888</value>
  </property>

  <!-- Mémoire des tâches Map -->
  <property>
    <n>mapreduce.map.memory.mb</n>
    <value>512</value>
  </property>

  <!-- Mémoire des tâches Reduce -->
  <property>
    <n>mapreduce.reduce.memory.mb</n>
    <value>512</value>
  </property>

  <!-- CLASSPATH Hadoop pour les jobs MR -->
  <property>
    <n>mapreduce.application.classpath</n>
    <value>$HADOOP_MAPRED_HOME/share/hadoop/mapreduce/*:$HADOOP_MAPRED_HOME/share/hadoop/mapreduce/lib/*</value>
  </property>

</configuration>
